const test = require('ava')
const log = require('util').debuglog('babel-plugin-transform-inline')
const babel_plugin_transform_inline = require('../src')

test('description', t => {
  t.is(true, true)
})
